<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="result">

<select id = "selectUserInfo" parameterType= "int" resultType="com.project.vo.UserVo">
		<![CDATA[
			SELECT
			    u.user_no "userNo",
			    u.name name,
			    u.weight weight,
			    u.height height,
			    a.name "adminName"
			FROM
			    users u, users a
			WHERE
				u.admin_no = a.user_no
			AND u.user_no = #{userNo}
		]]>
	</select>


</mapper>
